name: 'Pulumi outputs'
description: 'Get outputs from pulumi applications for defined inputs'
inputs:
  pulumi-organization:
    description: 'Pulumi organization name (lowercase)'
    required: true
  pulumi-project:
    description: 'Pulumi project (lowercase)'
  pulumi-stack:
    description: 'Pulumi project stack'
    required: true
  pulumi-resource:
    description: 'Requested project resource name'
    required: true
  pulumi-access-token:
    description: 'Pulumi access token'
    required: true
outputs:
  resource-output:
    description: 'The requested resource output from Pulumi'
runs:
  using: 'node16'
  main: 'index.js'
